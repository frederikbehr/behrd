import 'package:behrd/behrd.dart';
import 'package:flutter/material.dart';

class TextSettingWidget extends StatelessWidget {
  final TextSetting setting;
  const TextSettingWidget({super.key, required this.setting});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 100, height: 50, child: const Placeholder());
  }
}
