import 'package:behrd/behrd.dart';
import 'package:flutter/material.dart';

class StringSelectionSettingWidget extends StatelessWidget {
  final StringSelectionSetting setting;
  const StringSelectionSettingWidget({super.key, required this.setting});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: 100, height: 50, child: const Placeholder());
  }
}
